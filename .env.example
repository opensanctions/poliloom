# Docker image tags for production builds
API_IMAGE_TAG=latest
GUI_IMAGE_TAG=latest

# Wikidata dump file paths
WIKIDATA_DUMP_COMPRESSED=/var/cache/wikidata/latest-all.json.bz2
WIKIDATA_DUMP_EXTRACTED=/var/cache/wikidata/latest-all.json

# Backup storage
POLILOOM_BACKUP_ROOT=gs://your-bucket-name/backup

# Meilisearch configuration (minimum 16 bytes)
MEILI_MASTER_KEY=your-secure-master-key-here
