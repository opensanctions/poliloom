repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.2
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: gui-lint-staged
        name: GUI lint-staged
        entry: bash -c 'cd poliloom-gui && npx lint-staged'
        language: system
        files: ^poliloom-gui/.*\.(ts|tsx|js|jsx|json|css|md)$
        pass_filenames: false

      - id: gui-typecheck
        name: GUI TypeScript
        entry: bash -c 'cd poliloom-gui && npm run typecheck'
        language: system
        files: ^poliloom-gui/.*\.(ts|tsx)$
        pass_filenames: false
