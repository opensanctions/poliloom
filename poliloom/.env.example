# Database configuration
# For development, you can use SQLite (no credentials needed)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/poliloom

# For production, use PostgreSQL with credentials
# DATABASE_URL=postgresql://username:password@localhost:5432/poliloom

# OpenAI API configuration (required for LLM extraction)
# OPENAI_API_KEY=your_openai_api_key_here

# MediaWiki OAuth configuration (required for API authentication)
# MEDIAWIKI_CONSUMER_KEY=your_mediawiki_consumer_key
# MEDIAWIKI_CONSUMER_SECRET=your_mediawiki_consumer_secret

# Wikidata API configuration
# For testing (default): uses test.wikidata.org
# WIKIDATA_API_ROOT=https://test.wikidata.org/w/rest.php/wikibase/v1
# For production: uses www.wikidata.org
# WIKIDATA_API_ROOT=https://www.wikidata.org/w/rest.php/wikibase/v1

# CORS configuration (comma-separated list of allowed origins)
# For development, you can use "*" to allow all origins
# For production, specify actual frontend URLs
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080
